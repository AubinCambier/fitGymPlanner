meta {
  name: Set Status - désactiver
  type: http
  seq: 16
}

patch {
  url: {{baseUrl}}/api/v1/admin/users/{{targetUserId}}/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "is_active": false
  }
}
