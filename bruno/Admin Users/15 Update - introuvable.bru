meta {
  name: Update - introuvable
  type: http
  seq: 15
}

put {
  url: {{baseUrl}}/api/v1/admin/users/99999
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "first_name": "Fantôme"
  }
}

tests {
  test("status is 404", function() {
    expect(res.status).to.equal(404);
  });
  test("body status is error", function() {
    expect(res.body.status).to.equal("error");
  });
}
