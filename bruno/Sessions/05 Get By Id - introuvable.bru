meta {
  name: Get By Id - introuvable
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/sessions/99999
  body: none
  auth: none
}

tests {
  test("status is 404", function() {
    expect(res.status).to.equal(404);
  });
  test("body status is error", function() {
    expect(res.body.status).to.equal("error");
  });
}
