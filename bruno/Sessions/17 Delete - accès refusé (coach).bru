meta {
  name: Delete - accès refusé (coach)
  type: http
  seq: 17
}

delete {
  url: {{baseUrl}}/api/v1/sessions/{{sessionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{coachToken}}
}
