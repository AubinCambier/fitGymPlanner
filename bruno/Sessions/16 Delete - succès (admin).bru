meta {
  name: Delete - succès (admin)
  type: http
  seq: 16
}

delete {
  url: {{baseUrl}}/api/v1/sessions/{{sessionId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("status is 204", function() {
    expect(res.status).to.equal(204);
  });
}
