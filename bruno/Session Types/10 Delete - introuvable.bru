meta {
  name: Delete - introuvable
  type: http
  seq: 10
}

delete {
  url: {{baseUrl}}/api/v1/session-types/9999
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

tests {
  test("status is 404", function() {
    expect(res.status).to.equal(404);
  });
  test("body status is error", function() {
    expect(res.body.status).to.equal("error");
  });
}
