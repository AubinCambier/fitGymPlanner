meta {
  name: Set Status - réactiver
  type: http
  seq: 11
}

patch {
  url: {{baseUrl}}/api/v1/admin/pricing/{{pricingId}}/status
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "is_active": true
  }
}
