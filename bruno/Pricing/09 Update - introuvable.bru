meta {
  name: Update - introuvable
  type: http
  seq: 9
}

put {
  url: {{baseUrl}}/api/v1/admin/pricing/99999
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "label": "Tarif fantôme"
  }
}

tests {
  test("status is 404", function() {
    expect(res.status).to.equal(404);
  });
  test("body status is error", function() {
    expect(res.body.status).to.equal("error");
  });
}
