meta {
  name: Get Participants - accès refusé (pas propriétaire)
  type: http
  seq: 5
}

get {
  url: {{baseUrl}}/api/v1/coach/sessions/{{sessionId}}/participants
  body: none
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}
