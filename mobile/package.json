{
  "name": "fitgym-mobile",
  "version": "0.0.0",
  "private": true,
  "description": "FitGym Planner — Capacitor Android wrapper",
  "scripts": {
    "build:android": "cd ../frontend && npm run build -- --mode android && cd ../mobile && npx cap sync android",
    "open:android": "npx cap open android",
    "sync": "npx cap sync"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "typescript": "^6.0.3"
  }
}
