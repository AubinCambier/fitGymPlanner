import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'com.fitgym.planner',
  appName: 'FitGym Planner',
  webDir: '../frontend/dist',
}

export default config
