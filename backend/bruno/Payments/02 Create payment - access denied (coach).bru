meta {
  name: Create payment - access denied (coach)
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/v1/payments
  body: json
  auth: bearer
}

auth:bearer {
  token: {{coachToken}}
}

body:json {
  {
    "amount": 8.00,
    "description": "Session booking: Morning Yoga"
  }
}

tests {
  test("status is 403", function() {
    expect(res.status).to.equal(403);
  });
}
