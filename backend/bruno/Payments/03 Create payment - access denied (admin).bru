meta {
  name: Create payment - access denied (admin)
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/api/v1/payments
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "amount": 8.00,
    "description": "Session booking: Morning Yoga"
  }
}

tests {
  test("status is 403", function() {
    expect(res.status).to.equal(403);
  });
}
