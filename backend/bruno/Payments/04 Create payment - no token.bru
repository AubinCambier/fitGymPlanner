meta {
  name: Create payment - no token
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/api/v1/payments
  body: json
  auth: none
}

body:json {
  {
    "amount": 8.00,
    "description": "Session booking: Morning Yoga"
  }
}

tests {
  test("status is 401", function() {
    expect(res.status).to.equal(401);
  });
}
