meta {
  name: Create payment - invalid body
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/payments
  body: json
  auth: bearer
}

auth:bearer {
  token: {{authToken}}
}

body:json {
  {
    "description": "Missing amount field"
  }
}

tests {
  test("status is 400", function() {
    expect(res.status).to.equal(400);
  });
}
