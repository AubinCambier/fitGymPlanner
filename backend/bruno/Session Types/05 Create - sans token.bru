meta {
  name: Create - sans token
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/v1/session-types
  body: json
  auth: none
}

body:json {
  {
    "name": "Pilates"
  }
}

tests {
  test("status is 401", function() {
    expect(res.status).to.equal(401);
  });
  test("body status is error", function() {
    expect(res.body.status).to.equal("error");
  });
}
