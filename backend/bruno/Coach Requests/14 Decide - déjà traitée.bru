meta {
  name: Decide - déjà traitée
  type: http
  seq: 14
}

patch {
  url: {{baseUrl}}/api/v1/admin/requests/{{requestId}}
  body: json
  auth: bearer
}

auth:bearer {
  token: {{adminToken}}
}

body:json {
  {
    "status": "APPROVED"
  }
}

tests {
  test("status is 409", function() {
    expect(res.status).to.equal(409);
  });
  test("body status is error", function() {
    expect(res.body.status).to.equal("error");
  });
}
